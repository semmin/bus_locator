<!DOCTYPE html> 
<html> 
	<head> 
	<title>Bus Locator</title> 
	<meta name="viewport" content="width=device-width, initial-scale=1"> 
	<link rel="stylesheet" href="http://code.jquery.com/mobile/1.1.0-rc.1/jquery.mobile-1.1.0-rc.1.min.css" />
	<script src="http://code.jquery.com/jquery-1.7.1.min.js"></script>
	<script src="http://code.jquery.com/mobile/1.1.0-rc.1/jquery.mobile-1.1.0-rc.1.min.js"></script>
	
	<script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"></script>
    <script type="text/javascript" src="jquery.xdomainajax.js"></script>
	
<script type="text/javascript">
    //Declare global variables for latitude and longitude
    var latitude;
    var longitude;

    $(document).ready(function () {
        //Wire up search button click event handler
        $('#buttonSearch').click(function () {
            //Get the destination from the text input and URL encode it
            var destination = encodeURI($('#textDestination').val());
            //Create URL for google maps that will find the best route
            var url = "http://maps.google.com/maps?saddr=" + latitude + "," + longitude + "&daddr=" + destination + "&dirflg=r";
            //Use jQuery to get an HTML document from google maps using the URL
            $.get(url, function (data) {
                //Create a jQuery object using the response text
                var googleDoc = $(data.responseText);
                //Find the route number from the document
                ///In order to do this, look for an html element that has the transit line style and get the inner HTML
                var routeNumber = googleDoc.find('.trtline-name').html();
                //For testing, display an alert with the route number
                alert("The best route is " + routeNumber);
            });
        });

        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(success, error);
        } else {
            error('Geolocation not supported');
        }
        //});
    });


    function success(position) {
        latitude = position.coords.latitude;
        longitude = position.coords.longitude;

<<<<<<< HEAD
        $(document).ready(function() {
            
            if (navigator.geolocation) {
                navigator.geolocation.getCurrentPosition(success, error);
            } else {
                error('Geolocation not supported');
            }
            
        });  


        function success(position) {
			$('input[name=userLat]').val(position.coords.latitude);
			$('input[name=userLong]').val(position.coords.longitude);
=======
>>>>>>> Implemented the functionality to take the destination from the user and find the recommended route from Google Maps.  Since transit directions are not included in the Google Maps API, needed to scrape the site in order to find the route.  Included a script that enables jQuery to make cross domain requests.  Displayed an alert with the route for testing purposes.
          var latlng = new google.maps.LatLng(position.coords.latitude, position.coords.longitude);
          var myOptions = {
            zoom: 15,
            center: latlng,
            mapTypeControl: false,
            navigationControlOptions: {style: google.maps.NavigationControlStyle.SMALL},
            mapTypeId: google.maps.MapTypeId.ROADMAP
          };
          var mapcanvas = $('#mapcanvas');
          var map = new google.maps.Map(mapcanvas[0], myOptions);
          var marker = new google.maps.Marker({
              position: latlng, 
              map: map, 
              title:"I am here!"
          });
        }
        

        function error(msg) {
          var errMsg = typeof msg == 'string' ? msg : "Geolocation failed";
          $('#msg').html(errMsg);
        }

    </script>

</head>
<body>
    <div data-role="page">
        <div data-role="header">
            <h3>HRT Bus Report</h3>
        </div>
        <div data-role="content">
<<<<<<< HEAD
            <%= render partial: "layouts/errors_for", locals: { object: @report || Report.new } %>
            <form action="/reports" method="post" data-ajax="false">
                <label for="routeNumber">Route Number</label>
                <input id="routeNumber" name="routeNumber" type="text" value=""/>

                <label for="comments">Comments</label>
                <textarea id="comments" name="comments" value=""></textarea>

                <input type="hidden" name="userLat" value="">
                <input type="hidden" name="userLong" value="">
                
                <input type="submit" value="Report" />
=======
            <form url="www.google.com" action="POST">
                Destination
                <input id="textDestination" type="text" value=""/>
                <input id="buttonSearch" type="button" 
                    value="search" />
            
                <fieldset data-role="controlgroup">
                    <legend>Report Type</legend>
                        <input type="radio" name="reportType" id="radioOntime" value="ontime" checked="checked" />
                        <label for="radioOntime">On Time</label>

                        <input type="radio" name="reportType" id="radioLate" value="late"  />
                        <label for="radioLate">Late</label>

                        <input type="radio" name="reportType" id="radioEarly" value="early"  />
                        <label for="radioEarly">Early</label>

                        <input type="radio" name="reportType" id="radioOther" value="other"  />
                        <label for="radioOther">Other</label>
                </fieldset>

                <label for="comments">Comments</label>
                <textarea id="comments" value=""></textarea>&nbsp;
>>>>>>> Implemented the functionality to take the destination from the user and find the recommended route from Google Maps.  Since transit directions are not included in the Google Maps API, needed to scrape the site in order to find the route.  Included a script that enables jQuery to make cross domain requests.  Displayed an alert with the route for testing purposes.
            </form>

            <div id="msg"></div>
            <div id="mapcanvas" style="height: 200px; width: 200px;"></div>
        </div>
    </div>
</body>
</html>